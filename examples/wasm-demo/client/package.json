{
  "name": "@orion/wasm-demo-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "copy-wasm": "mkdir -p wasm && cp node_modules/orion-v2-lattigo/wasm/lattigo.wasm wasm/ && cp node_modules/orion-v2-lattigo/wasm/wasm_exec.js wasm/",
    "build:ts": "esbuild client.ts --bundle --platform=browser --format=esm --outfile=client.js",
    "build": "npm run copy-wasm && npm run build:ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "orion-v2-lattigo": "^6.2.4"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
